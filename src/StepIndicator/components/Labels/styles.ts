import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingVertical: 10,
  },
  itemContainer: {
    flex: 1,
    alignItems: 'center',
  },
  item: {
    textAlign: 'center',
  },
});
