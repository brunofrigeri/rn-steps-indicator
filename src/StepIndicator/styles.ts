import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  centered: {
    alignItems: 'center',
  },
  horizontalContainer: {
    flexDirection: 'row',
  },
  verticalContainer: {
    flexDirection: 'column',
  },
  progressBar: {
    position: 'absolute',
  },
  progressFinishedBar: {
    position: 'absolute',
  },
});
